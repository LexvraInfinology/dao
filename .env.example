# ==============================================================================
# B-TITAN PROTOCOL — ENVIRONMENT CONFIGURATION
# ==============================================================================

# Node Environment
NODE_ENV=development
PORT=4000

# ------------------------------------------------------------------------------
# 1. DATABASE CONFIGURATION (PostgreSQL)
# ------------------------------------------------------------------------------
POSTGRES_USER=btitan_admin
POSTGRES_PASSWORD=btitan_secret_password
POSTGRES_DB=btitan_db
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Prisma connection string (used by backend and indexer)
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# ------------------------------------------------------------------------------
# 2. REDIS CONFIGURATION (Cache, SIWE Nonce, BullMQ)
# ------------------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=btitan_redis_secret
REDIS_URL="redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}"

# ------------------------------------------------------------------------------
# 3. SECURITY & AUTHENTICATION (SIWE / JWT)
# ------------------------------------------------------------------------------
JWT_SECRET=super_secret_btitan_jwt_key_min_32_chars_long
JWT_EXPIRES_IN=24h
SIWE_NONCE_TTL_SECONDS=300
CORS_ORIGIN=http://localhost:3000

# ------------------------------------------------------------------------------
# 4. BLOCKCHAIN & RPC NETWORKS
# ------------------------------------------------------------------------------
# Chain ID (e.g., 31337 for Hardhat, 97 for BSC Testnet, 56 for BSC Mainnet)
CHAIN_ID=31337
RPC_URL=http://127.0.0.1:8545

# Contract Addresses (Populated from deployment output)
NEXT_PUBLIC_REGISTRY_ADDRESS=""
NEXT_PUBLIC_TOKEN_ADDRESS=""
NEXT_PUBLIC_NFT_ADDRESS=""
NEXT_PUBLIC_VESTING_ADDRESS=""
NEXT_PUBLIC_DAO_ADDRESS=""
NEXT_PUBLIC_MATRIX_ADDRESS=""

# Deployer / Admin Private Key (Keep strictly secret)
DEPLOYER_PRIVATE_KEY=""
INDEXER_POLL_INTERVAL_MS=3000
INDEXER_START_BLOCK=0
